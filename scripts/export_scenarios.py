"""Scenario files are already stored in `data/scenarios/`. This placeholder script exists so the full paper can reference a dedicated export utility if the app is later modularized."""
print('Scenarios are already externalized in data/scenarios/.')
